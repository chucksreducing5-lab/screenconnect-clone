# macOS/CocoaPods note

The current repo content appears to be:
- Node web server + browser UI
- Windows native projects: `native-agent/` and `native-service/`

No CocoaPods/Xcode artifacts are present here (e.g., no `Podfile`, no `Agent.xcworkspace`, no `check_pod_frameworks.sh`).

If macOS support exists for this product, it should live in a separate repository or a missing subfolder that needs to be added.

